import 'package:flutter/material.dart';

class ReviewsMain extends StatelessWidget {
  const ReviewsMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('reviews')));
  }
}
